-- 🗄️ إنشاء قاعدة البيانات مع Collation متوافق
-- هذا الملف متوافق مع جميع إصدارات MySQL/MariaDB

-- حذف قاعدة البيانات إذا كانت موجودة (اختياري - احذر!)
-- DROP DATABASE IF EXISTS `whats`;

-- إنشاء قاعدة بيانات جديدة مع collation متوافق
CREATE DATABASE IF NOT EXISTS `whats` 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- استخدام قاعدة البيانات
USE `whats`;

-- ملاحظة: بعد إنشاء قاعدة البيانات، قم بتشغيل migrations:
-- php artisan migrate

